name: fuzzy_search
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  diacritic: ^0.1.3

dev_dependencies:
  import_sorter: ^4.5.0
  pedantic: ^1.9.0
  test: ^1.14.4
