name: fuzzy_search
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  tuple: ^2.0.0

dev_dependencies:
  import_sorter: ^4.5.0
  pedantic: ^1.9.0
  test: ^1.14.4
